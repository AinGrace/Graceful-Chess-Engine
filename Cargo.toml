[workspace]
resolver = "3"
members = [
   "types",
   "lookup",
   "position"
]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
# panic = "abort"
strip = true
debug = 0
incremental = false
overflow-checks = false

[profile.profiling]
inherits = "release"
debug = 1
strip = false
