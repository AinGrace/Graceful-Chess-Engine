[package]
name = "position"
version = "0.1.0"
edition = "2024"

[dependencies]
types = { path = "../types"}
lookup = { path = "../lookup"}

[dev-dependencies]
rand = "0.10.1"
rayon = "1.11.0"
shakmaty = "0.30.0"

